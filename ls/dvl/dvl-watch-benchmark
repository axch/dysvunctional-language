#!/bin/bash

SELFDIR=$(dirname $0)

if [ `uname -m` == 'x86_64' ]; then
    HEAP=160000
    STACK=40000
else
    HEAP=6000
    STACK=2000
fi

exec mit-scheme --heap $HEAP --stack $STACK --batch-mode --no-init-file --load "${SELFDIR}/load.scm" --eval "(begin (pp (dvl-benchmark \"$1\")) (%exit 0))"
